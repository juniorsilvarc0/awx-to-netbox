---
# Configurações padrão AWX
awx_url: "http://10.0.100.159:8013"
# awx_user e awx_password são injetados via AWX Custom Credential Type

# Configurações padrão de filtros
default_inventory_filter: "VMware Inventory"
default_host_filter: ""

# Configurações de execução padrão
default_list_only: true
default_show_details: true
default_dry_run: true

# Configurações NetBox (para próximas etapas)
# netbox_url: "http://your-netbox-server:8000"
# netbox_token será injetado via AWX Credential quando configurado